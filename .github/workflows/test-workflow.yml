# Test workflow to validate syntax - can be deleted after first successful run
name: Test Workflow Syntax

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Test
        run: echo "✅ GitHub Actions workflow syntax is valid!"